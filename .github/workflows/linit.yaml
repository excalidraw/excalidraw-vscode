name: Lint

on: pull_request

jobs:
  lint-extension:
    name: Lint
    defaults:
      run:
        working-directory: ./extension
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: npm ci
      - run: npm run lint

